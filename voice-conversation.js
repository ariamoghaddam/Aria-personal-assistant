(function(){
  if(window.__ARIA_VOICE_CONVERSATION_V1)return;window.__ARIA_VOICE_CONVERSATION_V1=true;
  const $=id=>document.getElementById(id);
  let rec=null,listening=false,lastAnswer='';
  const faDigits=s=>String(s||'').replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[+d]);
  function ensureStyle(){if($('ariaVoiceTalkStyle'))return;const s=document.createElement('style');s.id='ariaVoiceTalkStyle';s.textContent=`#ariaVoiceTalkBtn{position:fixed;left:14px;bottom:calc(202px + env(safe-area-inset-bottom));z-index:2147483646;min-width:64px;height:54px;padding:0 14px;border:0;border-radius:18px;background:linear-gradient(135deg,#7c4dff,#3f8cff);color:#fff;font-weight:900;font-size:15px;box-shadow:0 12px 32px rgba(0,0,0,.5)}#ariaVoiceTalkDialog .voiceOrb{width:82px;height:82px;border-radius:50%;display:grid;place-items:center;margin:12px auto;background:linear-gradient(135deg,#7c4dff,#2dd4bf);font-size:34px;box-shadow:0 12px 35px #0008}#ariaVoiceTalkDialog .voiceLine{background:#0d151c;border:1px solid var(--line);border-radius:14px;padding:12px;min-height:58px;white-space:pre-wrap;line-height:1.8}#ariaVoiceTalkDialog .voiceActions{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}#ariaVoiceTalkDialog .listening{animation:ariaPulse 1s infinite alternate}@keyframes ariaPulse{from{transform:scale(.96);opacity:.75}to{transform:scale(1.05);opacity:1}}`;document.head.appendChild(s)}
  function voices(){try{return speechSynthesis.getVoices()||[]}catch{return[]}}
  function pickVoice(){const vs=voices();return vs.find(v=>/^fa(-|_)/i.test(v.lang))||vs.find(v=>/persian|farsi/i.test(v.name))||vs.find(v=>/^ar(-|_)/i.test(v.lang))||vs[0]||null}
  function speak(text){if(!('speechSynthesis'in window)||!text)return;try{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(String(text).replace(/[✦✓•]/g,'').replace(/\n+/g,'. '));u.lang='fa-IR';u.rate=.98;u.pitch=1;const v=pickVoice();if(v)u.voice=v;speechSynthesis.speak(u)}catch(e){console.warn('ARIA speak',e)}}
  function setState(t){const e=$('ariaVoiceTalkState');if(e)e.textContent=t}
  function setTranscript(t){const e=$('ariaVoiceTranscript');if(e)e.textContent=t||'—'}
  function setAnswer(t){lastAnswer=t||'';const e=$('ariaVoiceAnswer');if(e)e.textContent=t||'—'}
  function brainReady(){return window.ARIA_BRAIN&&typeof window.ARIA_BRAIN.ask==='function'}
  async function waitBrain(ms=5000){const start=Date.now();while(Date.now()-start<ms){if(brainReady())return true;await new Promise(r=>setTimeout(r,120))}return false}
  async function ask(q){q=String(q||'').trim();if(!q)return;setTranscript(q);setState('در حال فکر کردن…');setAnswer('');try{if(!(await waitBrain()))throw new Error('مغز ARIA هنوز آماده نشده.');const r=await window.ARIA_BRAIN.ask(q);const text=String(r?.text||'پاسخی دریافت نشد.').trim();setAnswer(text);setState(r?.actions?.length?'پاسخ آماده است؛ برای تغییرات، تأیید لازم است.':'پاسخ آماده است.');speak(text);if(r?.actions?.length){const b=$('ariaVoiceOpenAI');if(b)b.style.display='inline-block'}}catch(e){const m='نتونستم پاسخ مطمئن بگیرم. '+(e?.message||'خطای نامشخص');setAnswer(m);setState('خطا');speak(m)}}
  function stop(){if(!rec)return;try{rec.stop()}catch{}listening=false;$('ariaVoiceOrb')?.classList.remove('listening');setState('شنیدن متوقف شد.')}
  function start(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){setState('تشخیص گفتار زنده روی این دستگاه در دسترس نیست.');return}try{speechSynthesis?.cancel()}catch{};if(listening)return stop();rec=new SR();rec.lang='fa-IR';rec.interimResults=true;rec.continuous=false;rec.maxAlternatives=1;listening=true;$('ariaVoiceOrb')?.classList.add('listening');setState('دارم گوش می‌دم…');let final='';rec.onresult=e=>{let interim='';for(let i=e.resultIndex;i<e.results.length;i++){const t=e.results[i][0]?.transcript||'';if(e.results[i].isFinal)final+=t;else interim+=t}setTranscript((final||interim).trim())};rec.onerror=e=>{listening=false;$('ariaVoiceOrb')?.classList.remove('listening');setState(e.error==='not-allowed'?'دسترسی میکروفون بسته است.':'خطا در شنیدن صدا؛ دوباره بزن.')};rec.onend=()=>{listening=false;$('ariaVoiceOrb')?.classList.remove('listening');const q=($('ariaVoiceTranscript')?.textContent||'').trim();if(q&&q!=='—')ask(q);else setState('چیزی نشنیدم؛ دوباره امتحان کن.')};rec.start()}catch(e){listening=false;setState('میکروفون شروع نشد.')}}
  function ensureUI(){ensureStyle();if(!$('ariaVoiceTalkBtn')){const b=document.createElement('button');b.id='ariaVoiceTalkBtn';b.type='button';b.textContent='🎧 گفتگو';b.onclick=open;(document.body||document.documentElement).appendChild(b)}if($('ariaVoiceTalkDialog'))return;const d=document.createElement('dialog');d.id='ariaVoiceTalkDialog';d.innerHTML=`<div><div class="sectionHead"><b>🎧 گفت‌وگوی صوتی با ARIA</b><button id="ariaVoiceTalkClose" class="ghost" type="button">بستن</button></div><div class="sub" style="margin:8px 0">هر چیزی درباره کارها، برنامه، زمان آزاد، اولویت‌ها و یادآورها بپرس. ARIA از اطلاعات داخل برنامه جواب می‌دهد و پاسخ را با صدا می‌خواند.</div><div id="ariaVoiceOrb" class="voiceOrb">🎙</div><div class="voiceActions" style="justify-content:center"><button id="ariaVoiceListen" class="primary" type="button">شروع صحبت</button><button id="ariaVoiceRepeat" type="button">🔊 دوباره بخوان</button></div><div class="sub" style="margin-top:12px">چیزی که شنیدم</div><div id="ariaVoiceTranscript" class="voiceLine">—</div><div class="sub" style="margin-top:10px">پاسخ ARIA</div><div id="ariaVoiceAnswer" class="voiceLine">—</div><div id="ariaVoiceTalkState" class="sub" style="margin-top:8px">آماده‌ام.</div><div class="voiceActions"><button id="ariaVoiceOpenAI" type="button" style="display:none">باز کردن AI برای تأیید تغییرات</button></div></div>`;document.body.appendChild(d);$('ariaVoiceTalkClose').onclick=()=>{stop();try{speechSynthesis.cancel()}catch{};try{d.close()}catch{d.removeAttribute('open')}};$('ariaVoiceListen').onclick=start;$('ariaVoiceRepeat').onclick=()=>speak(lastAnswer);$('ariaVoiceOpenAI').onclick=()=>{try{d.close()}catch{};window.ARIA_BRAIN?.open?.()};}
  function open(){ensureUI();const d=$('ariaVoiceTalkDialog');try{d.showModal()}catch{d.setAttribute('open','')}setState('آماده‌ام؛ بزن و صحبت کن.')}
  window.ARIA_VOICE_CONVERSATION={open,ask,speak,start};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(ensureUI,900));else setTimeout(ensureUI,900);
})();